{
  "name": "floe",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/engine packages/cli && npm run build:app",
    "build:app": "npm --workspace @floe/app run build",
    "floe": "node packages/cli/dist/main.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
